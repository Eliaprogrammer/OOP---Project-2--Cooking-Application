// Cooking interface: Four methods that must be implemented in the each subclass.
public interface Cooking {

    //Creating empty methods to use in each subclass.
    String prepareDish();
    String cook();
    String serveDish();
    String dishComplete();
}
